<div class="container">
  <div class="row">
    <div class="col-sm-6">
      <h1>Users</h1>
      <%= cl_image_tag @user.photo, height: 300, width: 400, crop: :fill %>
      <h3>Teacher's Name: </h3>
      <h3><%= @user.first_name %>  <%= @user.last_name %></h3>
      <h3><%= @user.email %></h3>
      <h3><%= @user.photo %></h3>
      <%= link_to "Create Lesson", new_lesson_path, class: "btn btn-danger" %>
    </div>
  </div>
</div>